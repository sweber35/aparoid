{
  "name": "aparoid",
  "version": "0.1.0",
  "bin": {
    "aparoid": "bin/aparoid.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "datalake": "AWS_PROFILE=aparoid-dev npx cdk deploy StorageStack GlueStack ProcessingStack TestFilesStack --require-approval never",
    "frontend": "AWS_PROFILE=aparoid-dev npx cdk deploy FrontendStack  --require-approval never && cd frontend && ./deploy.sh"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1020.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.202.0",
    "constructs": "^10.0.0",
    "dotenv": "^17.2.0"
  }
}
